---
title: VPS 当前基线清单
permalink: fwq/
date: 2026-04-20 14:40:59
published: false
categories:
tags:
desc: 服务器
---

# VPS 当前基线清单

## 一、服务器定位
这台 VPS 当前承担两条线：

1. **OpenClaw 工作流服务器**
2. **自建 Xray 代理节点服务器**

当前主状态是：  
**两条线都在，但代理节点已经成功打通并可用。**

---

## 二、网络与域名
- 主公网 IP：`64.23.159.110`
- Reserved IP：`209.38.118.191`
- 域名：`070902.xyz`
- 域名当前指向：`209.38.118.191`

说明：
- 外部访问域名时，是通过 Reserved IP 进入当前这台机
- 这套映射目前已验证可用

---

## 三、防火墙
当前 `ufw` 已开启，并放行：

- `22/tcp`
- `80/tcp`
- `443/tcp`

当前防火墙结论：

- SSH 正常
- HTTP 正常
- HTTPS / Xray 入口正常

---

## 四、证书与 TLS
当前已经完成：

- `070902.xyz` 的 Let's Encrypt 证书签发
- 443 TLS 正常
- Xray 已可正常读取证书

当前证书相关实际使用路径：

- 原始证书目录：`/etc/letsencrypt/live/070902.xyz/`
- Xray 使用副本目录：`/usr/local/etc/xray/certs/`

Xray 当前实际建议使用的证书文件：

- `/usr/local/etc/xray/certs/fullchain.pem`
- `/usr/local/etc/xray/certs/privkey.pem`

---

## 五、Xray 当前状态
### 已完成
- Xray 已安装
- 配置已通过测试
- 服务已启动成功
- 443 当前由 Xray 占用

### 关键文件
- 配置文件：`/usr/local/etc/xray/config.json`
- 建议备份：`/usr/local/etc/xray/config.json.working`

### 常用检查命令
```bash
systemctl status xray
ss -tlnp | grep :443
journalctl -u xray -n 50 --no-pager
journalctl -u xray -f


后面如果你断线、重装、迁移，最值钱的是记住这些：

服务端关键点
Xray 配置文件：/usr/local/etc/xray/config.json
证书副本目录：/usr/local/etc/xray/certs
服务状态检查：
systemctl status xray
ss -tlnp | grep :443
journalctl -u xray -n 50 --no-pager
客户端关键点
协议：trojan
域名：070902.xyz
端口：443
SNI：070902.xyz
密码：HE6WkuYtWqlDOuXJMD4uOtJMNKaOcVpr
微信 / 腾讯系必须直连



