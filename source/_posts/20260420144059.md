---
title: VPS 当前基线清单
permalink: fwq/
date: 2026-04-20 14:40:59
published: false
categories:
tags:
desc: 服务器
---

后面如果你断线、重装、迁移，最值钱的是记住这些：

服务端关键点
Xray 配置文件：/usr/local/etc/xray/config.json
证书副本目录：/usr/local/etc/xray/certs
服务状态检查：
systemctl status xray
ss -tlnp | grep :443
journalctl -u xray -n 50 --no-pager
客户端关键点
协议：trojan
域名：070902.xyz
端口：443
SNI：070902.xyz
密码：HE6WkuYtWqlDOuXJMD4uOtJMNKaOcVpr
微信 / 腾讯系必须直连



一、服务器定位

这台 VPS 当前承担两条线：

OpenClaw 工作流服务器
自建 Xray 代理节点服务器

当前主状态是：
两条线都在，但代理节点已经成功打通并可用。

二、网络与域名
主公网 IP：64.23.159.110
Reserved IP：209.38.118.191
域名：070902.xyz
域名当前指向：209.38.118.191

说明：

外部访问域名时，是通过 Reserved IP 进入当前这台机
这套映射目前已验证可用
三、防火墙

当前 ufw 已开启，并放行：

22/tcp
80/tcp
443/tcp

当前防火墙结论：

SSH 正常
HTTP 正常
HTTPS / Xray 入口正常
四、证书与 TLS

当前已经完成：

070902.xyz 的 Let's Encrypt 证书签发
443 TLS 正常
Xray 已可正常读取证书

当前证书相关实际使用路径：

原始证书目录：/etc/letsencrypt/live/070902.xyz/
Xray 使用副本目录：/usr/local/etc/xray/certs/

Xray 当前实际建议使用的证书文件：

/usr/local/etc/xray/certs/fullchain.pem
/usr/local/etc/xray/certs/privkey.pem
五、Xray 当前状态
已完成
Xray 已安装
配置已通过测试
服务已启动成功
443 当前由 Xray 占用
关键文件
配置文件：/usr/local/etc/xray/config.json
建议备份：/usr/local/etc/xray/config.json.working
常用检查命令
systemctl status xray
ss -tlnp | grep :443
journalctl -u xray -n 50 --no-pager
journalctl -u xray -f
六、代理协议状态

当前服务端方案是：

Xray
trojan
TLS
443 端口
域名 SNI = 070902.xyz

客户端已验证可连通。

注意：

Xray 日志已提示 trojan 方案可用，但不算长期最优
当前阶段先保持不动，后续再考虑升级方案
七、nginx 当前状态
nginx 仍安装在系统中
但当前不是 443 的实际提供者
为了让 Xray 接管 443，nginx 已让出这条链路

当前结论：

现在这台机优先保障的是代理节点可用
不是“网站 + 代理共存”模式
八、客户端 Clash 当前基线

当前客户端已成功连通。

基本参数
协议：trojan
服务器：070902.xyz
端口：443
SNI：070902.xyz
密码：与 /usr/local/etc/xray/config.json 中一致
当前策略要点
微信 / 腾讯系直连
国内流量直连
常用国外服务走代理
避免再使用“所有流量全走代理”的粗暴规则
九、已解决问题

这条线目前已经解决过的核心问题包括：

域名和服务器 IP 关系理顺
443 未监听问题
nginx 未配置 HTTPS 问题
TLS 握手失败问题
Xray 证书读取权限问题
Clash 节点 timeout 问题
微信被错误代理导致无法联网问题
十、当前遗留优化项

这些不是现在的阻塞项，但后面可以做：

1. Xray 运行用户不够规范

当前服务里使用的是 User=nobody。
能跑，但不优雅，后续建议改成专用用户。

2. trojan 方案偏过渡态

当前能用，但长期看可考虑迁到更现代的方案。

3. nginx 与 Xray 未做共存设计

现在是“优先代理节点”的方案。
如果以后要网站和代理共存，需要单独设计。

4. 证书续期链路后续要复查

现在证书已能用，但未来如果 nginx 持续停用，证书续期方式要记得再确认一次。

十一、当前阶段建议
现在不要乱动的
Xray 配置核心结构
443 端口归属
Clash 已验证通过的最小配置
微信直连规则
现在应该做的
备份当前 working 配置
记录 trojan 密码
保留一份客户端配置
重启后再复测一次节点可用性